#ifndef UTILS_HPP
# define UTILS_HPP

#include <vector>
#include <string>

std::vector<std::string> split(std::string str, char delimiter);
long ft_numlen(long num);
long ft_expandrf(float num);
long ft_roundf(float x);
std::string ft_ltos(long s);

#endif //UTILS_HPP